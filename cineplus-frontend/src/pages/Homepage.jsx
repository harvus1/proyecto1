import React from 'react';
import { Typography } from '@mui/material';

const HomePage = () => (
  <div>
    <Typography variant="h3">Bienvenido a CinePlus</Typography>
  </div>
);

export default HomePage;